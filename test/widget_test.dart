import 'package:flutter_test/flutter_test.dart';

import 'package:crewclock/main.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const CrewClockApp(), isA<CrewClockApp>());
  });
}
